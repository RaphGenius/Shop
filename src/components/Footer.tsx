import React from "react";

type Props = {};

const Footer = (props: Props) => {
  return (
    <footer className="w-full h-[100px] absolute bottom-0 bg-secondary100">
      azeazez
    </footer>
  );
};

export default Footer;
